<div>
    <nav class="navbar navbar-expand-md navbar-dark bg-primary">
        <a class="navbar-brand" href="{{ url("app_index") }}">EB-Bid</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ url("app_index") }}">Home <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url("book_index") }}">Books</a>
                </li>
            </ul>

        </div>
        <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">

            <form class="form-inline">
                <input class="form-control" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-success ml-1" type="submit">Search</button>
            </form>
            <ul class="navbar-nav ml-auto">
                {% if not is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url("app_login") }}" >Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url("app_register") }}" >Register</a>
                    </li>
                {% else %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link disabled" href="{{ url("app_admin") }}">Admin Panel</a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url("user_show", {'id': app.user.id}) }}" >Profile</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url("app_logout") }}" >Logout</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>
</div>